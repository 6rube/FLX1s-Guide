# Information
Chromium is currently broken when installed via flatpak (07.01.2026)
It is reccomended to install it via apt.

# Installation of Chromium Browser

1. Install chromium
```bash
sudo apt install chromium
```
7. Go to Mobile Settings App (Do not close chromium)
8. Go to Compositor and enable down scale for brave